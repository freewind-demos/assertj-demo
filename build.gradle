apply plugin: 'java'

repositories {
    mavenLocal()
    jcenter() // fast!
    mavenCentral() // very slow
}

dependencies {
    testCompile(
            'junit:junit:4.11',
            'org.assertj:assertj-joda-time:2.0.0'
    )
}
